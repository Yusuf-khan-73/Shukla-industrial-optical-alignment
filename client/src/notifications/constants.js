/** Notification timing — fade in, hold, fade out (milliseconds). */
export const NOTIFY_ENTER_MS = 300;
export const NOTIFY_HOLD_MS = 3000;
export const NOTIFY_EXIT_MS = 300;
export const NOTIFY_MAX_VISIBLE = 3;
